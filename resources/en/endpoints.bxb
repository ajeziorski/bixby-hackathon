endpoints {
  action-endpoints {
    action-endpoint (Describe) {
      accepted-inputs (objectName)
      local-endpoint (Describe.js)
    }
    action-endpoint (Use) {
      accepted-inputs (usedObjectName, usedOnObjectName)
      local-endpoint (Use.js)
    }
  }
}
